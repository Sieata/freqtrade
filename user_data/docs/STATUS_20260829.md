# 2026-08-29 工作收敛总账

> 本文档 = 当日全部工作的收敛快照（本会话 + 并行会话）。细节出处：RESEARCH.md 十~十五、
> 各 FREEZE 文档、user_data/reports/logs/。此后新会话以本文为"当前状态"入口。

## 一、基础设施（本机从零到可用）

- Windows 设备环境建成：uv + Python 3.12.14 + freqtrade 2026.8-dev（TOP10 全量数据随库分发）。
- 数据保鲜一键：`refresh_all.sh`（K线/funding 增量 + OI 累积 + metrics 月度增量）。
- 工具集齐：`validate_strategy`（--pool top10 + 年化/逐年% 自动输出）、`tier_b_eval`、
  `portfolio_full`、`signal_recon`（FREEZE 月度对账）、`paper_status`（泛化至 FS/BigMove）、
  `cross_venue_scanner`（跨所极端费差）、`oi_accumulate`（OI 历史累积）、审计双工具。

## 二、策略库现状（权威表）

| 策略 | 定位 | TOP10 表现 | 门禁 | 状态 |
|---|---|---|---|---|
| **WeekendReverseV2** | 引擎 | 六段全正 +19.2/+16.6% 年 | ✅ 全过 | **paper 进行中（FREEZE）** |
| WeekendReverseV1 | 备份 | 略弱于 V2 | ✅ | 退役建议（V2 全覆盖） |
| **BigMoveV1** | Tier B 臂 | VAL +17.0% 年 · PF 3.21 | Tier B ✅ | **paper 待启动（包已建）** |
| **FS（V1L）** | Tier B 臂（中小盘倾向） | VAL +9.8% 年 · ZEC 79% | Tier B ⚠️（TEST 最差月 1.52x） | **paper 待启动（包已建）**；持有期研究（并行会话）不晋级、维持 h=72 |
| **OIFlushV2** | Tier B 臂（OI 事件） | VAL +6.2% 年 · 2026 -11% | Tier B ✅ | **12 月初 paper**（等 OI 历史 60 天，累积中 31 天） |
| CrashBuyV1 | 观察名单 | 2026 -24.9% | ❌ | 退出实盘候选，恢复 = 连续两季 TOP10 为正 |
| OIFlushV1 / carry(H7·H8·H8c) | 归档 | — | — | 重启路径已写入 RESEARCH |
| 代码审计 | — | — | — | 6 策略 lookahead/recursive 双工具 + 人工全过，**无致异常盈亏 bug** |

## 三、当日研究结论速览

1. **FS 全流程闭环**：满池重验四腿全正（宽度 FAIL 但 ZEC 集中=小池假象）→ 蓝筹失效诊断 →
   TOP10 重切现出原形 → V1L live 安全变体 + 启动包 → Tier B 评估（仅最差月边缘）。
2. **OIFlush 复活线**：预注册 → 30 天动量过滤胜出（2022 -0.30%→+0.70%）→ V2 原型 →
   A/B 全过（含 2022≥0）→ 定版验证 TEST 全过（PF 3.84/4.15）→ VAL 2026 -11% 新体制失败 →
   Tier B 全过。**教训：体制依赖是结构性的，失败年份会漂移。**
3. **代码审计**：无致异常盈亏 bug；V2 高胜率=尾随机制；Fee=0.05%/边 worst-case。
4. **套利族（carry 全家）**：机会真实但全部是"机会驱动+衰减窗口"形态——
   H7 永续 carry（+7-10%/年，BNB 例外剔除）、H8 交割基差（100% 胜率但稀少）、
   H8c 永续×交割（并行会话：8-13%/年，**paper 模拟器已落地**）、
   H9 股票永续（对冲成本价目表：TENCENT +75%/SOXL +40%/SPY -26%；盘后定价权）、
   H10 跨所费差（**CXMT +368%/UNITREE +236% 年化 delta 中性，实时窗口**）+ 扫描器（首扫 34 窗口）。
5. **双信号叠加**：FS×OI 同 K 线共振 = 全库最高质量信号（+4.71%/笔、2022 转正）但 n=22
   → 定调加仓置信标记。
6. **四臂组合**：VAL 年均 +$5,566（V2 +1,993/BM +2,040/FS +1,128/OI +741），**逐年全正**，
   并发峰值 12 仓。

## 四、制度化（今日起生效）

1. **TOP10 市值池 = 第一评估口径**（实盘优先；CORE50/VOLUME 只作泛化面）。
2. **逐年收益率按"每年重置 $1,000 本金"**表达（跨年不复利不共用额度）+ 年化双口径（钱包/占仓）。
   权威定义：STRATEGY_WORKFLOW 第〇节 0.4。
3. **门禁分层提案**（草案待批）：Tier A 引擎现行门禁 / Tier B 事件臂 7 条组合增量门禁。
   评估表已实测（BigMove/OIFlush 全过、FS 差 0.02 裁量项）。
4. **OI 累积器 cron（paper 设备）**——本机不搞定时任务（用户指令）。

## 五、运行态与日历

| 时点 | 事项 | 归属 |
|---|---|---|
| 现在 | paper 设备 `git pull` → 启动 FS / BigMove（/ H8c 模拟器）+ OI cron | 用户 |
| 现在 | 门禁分层批准（阈值可改：+3pp / 1.5× / -15% / 重叠 30%） | 用户 |
| 每会话 | `refresh_all.sh` + OI 累积补跑 | 助手 |
| 12 月初 | OI 历史满 60 天 → OIFlushV2 paper 包（live 数据路径 + dry-run） | 助手 |
| paper 期间 | 月度信号对账（signal_recon）+ 周报（paper_status） | 助手 |
| 2027-02-28 | FS 评审（FREEZE_FS 判据 + TOP10 切片核算） | 助手 |
| 机会性 | 跨所费差窗口执行（扫描器告警 → 15.7 执行清单） | 用户决策 |

## 六、今日教训沉淀（详情见 ENGINEERING_NOTES）

1. `startup_candle_count` 双语义（主周期暖机数 + informative 拉取深度）——调大必须两头核算。
2. Hyperliquid 股票永续在 **builder dexes**，主 dex 查不到；ccxt 符号 `XYZ-CXMT/USDC:USDC`。
3. 资金费收付第一性原理：f>0 多头付——所有资金费计算必须由此核对（今日抓到一处符号反）。
4. 无止损事件研究系统性高估"接刀类"假设；原型化时止损宽度须与入场冲击联动。
5. 快照 predicted-funding 不可用于研究，必须 funding history realized 口径并按结算间隔年化。
6. ccxt limit=1000 覆写 feather——全史拉取须分页。
7. 跨所 carry 双币风险：USDC/USDT 正常期噪音（≤0.44%），尾部 SVB 型 -8.7%/98h（3.5 年一遇）。
