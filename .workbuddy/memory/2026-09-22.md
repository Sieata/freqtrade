# 2026-09-22

## CrashPortfolioV1 原型验证与独立臂关闭（RESEARCH 25）

- 实现 `user_data/strategies/CrashPortfolioV1.py`（SHA256[:16] 0129012e11b6d2f4）：
  无止损（stoploss -0.99）+ custom_exit 24h 定时离场 + informative_pairs 横截面排名。
  两个实现坑：① `pd.Series(vals, index=df["date"].values)` 是 object 索引 → map 全 NaN
  （首跑 0 笔），必须 `index=pd.DatetimeIndex(df["date"])`；② 策略类不能定义
  max_open_trades（strategy_resolver 115-123 用类属性覆盖 config，validate 要传池内品种数）。
- TEST 对账：259 笔笔数一致、253 笔逐笔一致（均值差 -0.002pp）；6 笔差异全是「同根
  离场再入场」边界（仿真允许 E'=X，freqtrade 多 4h 冷却，那 6 笔反而 +67pp）。语义等价。
- validate 判决：TEST 全过（259 笔 +401pp / PF 1.95 / 品种 8/8 / dd 7.2%）；VAL FAIL
  （品种 5/9=55.6%，2026 年 -106.2pp，ZEC 单笔 -46%）→ **独立臂关闭**（VAL 一次纪律）。
- 新工具 `arm_compare.py`（两臂对照：逐年/品种/入场重叠/共仓天数/日 PnL 相关/出场
  结构/尾部）。CrashBuyV2 对照判决：2026 年 -106.2 vs -19.7pp（差 86pp 全部来自收阳
  确认 + -8% 止损 + trailing）；TEST 段 CPV1 赚 2.8 倍（+401 vs +143pp）但尾部 -52%
  vs 全钉 -8%；日 PnL 相关 TEST r=+0.44 / VAL r≈+0.004；XMR 两臂共亏（-214/-153）。
  **确认腿的载重作用 = 24.6 止损悖论的正向验证**：CBV2 止损腿 VAL 付 -$2,069 换 2026
  存活；CPV1 不能带止损因为进场质量低。
- CPV2 复活假设（mom30 磨损市过滤，VAL 后设计有污染性）已记录 RESEARCH 25.5，不作数。
- 旧账第三轮提醒：21.1 期现资金费规则臂过门禁未实施；h8c_paper.py funding 符号未修。
