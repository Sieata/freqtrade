# 策略研发工作流

> 每步通过才能进入下一步。过拟合的策略直接丢弃。
> **2026-08-28 起实行标准化口径（第〇节）**：时间切分与币池的唯一权威来源在 `user_data/universe/`，
> 一键验证用 `validate_strategy.py`。之前的历史检测记录按旧口径执行，保留作参考。

---

## 第〇节：标准化口径（强制，所有新研究一律遵守）

### 0.1 时间切分（`user_data/universe/splits.json`，冻结不滚动）

| 集合 | timerange | 用途 |
|------|-----------|------|
| 暖机 | 2021 全年数据 | 只做指标预热（4h 250 根 / 1d MA200），**不计入可交易区间**（教训见 FREEZE_V2 §8） |
| **测试集 TEST** | `20220101-20240828` | 调参、对比、筛选、逐年滚动**只准用这段** |
| **验证集 VAL** | `20240828-` | 最近 2 年。**定版候选只跑一次**；跑过又改参 = 作废重来 |

纪律：
- 调参任何环节碰了 VAL 数据 = VAL 污染，该策略的 VAL 结论作废。
- VAL 只在"这是最终版吗？= 是"时跑。最终定版后还有 paper forward-test（真实时间前推）作为最后一关。
- 重切边界须人为 deliberate：改 `splits.json`（version+1）+ 在 RESEARCH.md 记录，旧结论标注旧版本。
- 工具：`.venv/bin/python user_data/scripts/time_splits.py`（打印可直接粘贴的 timerange）。

### 0.2 币池分层（`user_data/universe/pairs_core.txt` / `pairs_volume.txt`）

| 池 | 文件 | 定义 | 用途 |
|----|------|------|------|
| **CORE 实盘池** | `pairs_core.txt` | CoinGecko 市值 Top50 ∩ 币安 USDT-M 永续（剔稳定币/封装/商品） | **实盘/paper 只允许跑这个池** |
| **VOLUME 泛化池** | `pairs_volume.txt` | 币安 24h 成交量 Top30（剔稳定币/封装/股票/商品永续） | **只做泛化测试，禁实盘** |

纪律：
- 双榜同参都能过才算普适（见 4.1）；VOLUME 池过而 CORE 池不过 = edge 只在热门小币上，不实盘。
- 两份文件是**生成日快照**：重生成（`make_universe.py`）= 币池变更，需在 RESEARCH.md 记录。
- VOLUME 榜有幸存者偏差（今天热门 ≠ 历史热门）：结论必须结合报告里的单年集中度，防"新币单年 pump"假 edge（BigMove 研究里 ZEC/BANK 的教训）。
- 数据缺口补齐：`./ensure-data.sh user_data/universe/pairs_volume.txt`（新品种 funding 老数据走 import_funding_vision.py）。

### 0.3 一键验证

```bash
.venv/bin/python user_data/scripts/validate_strategy.py --strategy <名字>
  # 默认: 双池 × TEST+VAL，独立口径（每笔固定 $1,000，--max-open-trades=池内品种数）
  # 输出: 门禁 PASS/FAIL + 品种×年度矩阵 + markdown 报告（user_data/reports/）
  # 可选: --pool core|volume --config ... --fee 0.001 --skip-test/--skip-val
```

- 泛化验证一律用**独立口径**（固定 $1,000/笔），不受满仓复利路径影响；复利口径留给定版后的单池配置回测。
- `--cache none` 恒定（对账纪律）。

---

## 第一阶段：发现（1-2 轮）

### 1.1 数据观察
- 下载目标品种至少 4 年 4h K线数据
- 统计极端涨跌后的反转率（Python，不写策略）
- 对比不同时段（凌晨/周末/全时段）
- **出口标准**：找到至少一个五品种胜率 >55% 的信号

### 1.2 假设形成
- 一句话说清楚"什么情况下，为什么，往哪个方向做"
- 必须可证伪（"RSI<30 后 24h 涨"可以证伪，"RSI 有用"不行）
- **出口标准**：假设清晰到能直接翻译成 30 行代码

---

## 第二阶段：原型（2-5 轮）

### 2.1 最简实现
- ≤30 行策略代码
- 单一入场条件，不加过滤
- 固定止损 + 固定止盈，不加尾随
- **出口标准**：五品种至少 4/5 盈利

### 2.2 单参数迭代
- 每轮只改一个参数
- 对比新旧版本全量回测
- 劣化立即删除，改善才保留
- **出口标准**：连续 2 轮无改善即停止

---

## 第三阶段：过拟合检测（强制）

> **所有策略必须通过全部三项检测。任一项不通过即丢弃。**

### 3.1 盲参测试（A）
- 用完全不相关的参数组跑同一信号
- 例如：尾随策略 → 用 5% 激活/2% 步长代替精调的 1.2%/0.3%
- **通过标准**：更换参数后 5+ 品种仍全部盈利

### 3.2 参数稳定性（B）
- 核心参数在合理范围内扫描（如尾随步长 0.2%~0.8%）
- 在样本外测试集上跑
- **通过标准**：所有参数点都盈利，利润曲线平滑无尖峰

### 3.3 逐年滚动（C，只在测试集内滚动）
- 2021-2022 数据定参 → 跑 2023
- 2021-2023 数据定参 → 跑 2024
- **滚动到 2024 为止**；2024-08 之后属于验证集，按 4.3 一次性处理，不准进调参循环
- **通过标准**：每个滚动窗口都在测试年盈利

---

## 第四阶段：普适性验证（强制）

> 本阶段整体用 `validate_strategy.py` 执行（0.3），以下为各项通过标准。

### 4.1 双池验证（原"双榜"）
- **CORE 实盘池**（市值 Top50）与 **VOLUME 泛化池**（24h 成交量 Top30），两张榜单同一套参数
- 2026-08 起币安已有股票/商品永续（underlyingType≠COIN），make_universe.py 已自动剔除
- **通过标准**：两池独立口径下均 ≥80% 品种盈利且 PF>1.0

### 4.2 验证集 VAL（原"最近一年/样本外"，一次性）
- 在 TEST 上调参定版后，VAL（最近 2 年）**只跑一次**
- **通过标准**：≥80% 品种盈利、PF>1.0、利润>0、回撤 ≤30%
- VAL 跑挂 → 回 TEST 重新研究 → 新版本重新过全流程；**禁止拿 VAL 反复对比调参**
- 定版后仍有 paper forward-test（真实时间前推）作为最终一关

---

## 第五阶段：收尾

### 5.1 文档
- 一页纸简报：参数表 + 品种表 + 年度表
- 记录所有失败尝试（防止重复踩坑）

### 5.2 命名归档
- 策略名 + 版本号（如 `WeekendReverseV1.py`）
- 中间版本保留 2-3 个有代表性的
- 纯失败版本删除

### 5.3 组合评估
- 和现有策略的信号重叠度
- 组合后利润曲线是否更平滑

---

## 快速过拟合判断清单

| 症状 | 判断 |
|------|------|
| 只有一个品种赚钱 | 过拟合，丢 |
| 参数稍微一动就崩 | 过拟合，丢 |
| 逐年滚动有一年大亏 | 过拟合，丢 |
| 利润集中在一个品种/一年 | 过拟合，丢 |
| 全靠某个过滤条件活着 | 过拟合，丢 |

**金标准**：换参数还能赚钱 + 换年份还能赚钱 + 换品种还能赚钱 = 真策略。

---

## 已丢弃策略清单

### ETHShortV1_overfit 🧪 过拟合对照
```
丢弃原因：单品种过拟合反面案例，与 WeekendReverseV1 形成对比；源码已删（2026-08-28），结论保留
ETH +176%（仅在 ETH 上表现好）/ SOL +26% / BTC -23% / BNB -37% / DOGE -46%
本质：针对 ETH 单标的过拟合模板，EMA50 斜率/BTC 过滤/ATR 止损全为 ETH 定制
换品种全崩 —— 证明单品种优秀 ≠ 策略有效
```

### DOGEDualV1
```
丢弃原因：多品种崩盘（仅 DOGE 盈利）；源码已删（2026-08-28），结论保留
DOGE +185% / ETH +9% / SOL -7%
本质：8%/10% 涨跌阈值按 DOGE 波动率定制，换标的失效
```

### 凌晨闪崩系列（NightCrash V1-V2）
```
丢弃原因：统计优势存在但利润太薄（<$250），经不起交易摩擦
15m/30m/1h/4h 全周期尝试，无一突破 +$250 天花板
```

### FreqAI 机器学习
```
丢弃原因：训练数据不足，LightGBM 学不到有效模式（+0.06% 几乎为零）
```

### 周线趋势过滤、短期涨幅过滤、RSI 过滤等
```
丢弃原因：全部劣化原版，属于过拟合噪声
```

---

## 已完成检测 — 2026-08-05（旧口径：市值/交易量 Top10 + 2025-2026 样本外；保留作参考，新研究按第〇节执行）

### WeekendReverseV1 ✅ 正式策略

```
A 盲参：       ✅ 341笔 +$4,052（10/10盈）
B 参数稳定：    ✅ 步长 0.2%-0.8% 全盈
C 逐年滚动：    ✅ 2023-2026 全盈
市值 Top10：    ✅ 595笔 +$47,691（9/10盈）
交易量 Top10：  ✅ 564笔 +$74,102（9/10盈）
最近一年：      ⚠️ 212笔 +$2,146（6/10盈，弱于标准线但绝对正）
样本外：        ✅ 180笔 +$2,411（10/10盈）
结论：通过。最近一年偏弱与市场低波动环境相关，非策略失效。
```

### CrashBuyV1 ✅ 低频互补策略

```
A 盲参：       ✅ 113笔 +$469（5/5盈）
B 参数稳定：    ✅ 步长 1%-5% 全盈
C 逐年滚动：    ✅ 2023-2025全盈，2026 -$13（仅3笔）
市值 Top10：    ⚠️ 未测
交易量 Top10：  ⚠️ 187笔 +$987（7/10盈，新币失效）
最近一年：      ⚠️ 6笔 +$43（4/6盈）
结论：通过但信号太稀疏（年均20笔），定位为低频互补。
```

### ETHShortV1_overfit 🧪 过拟合对照

```
丢弃原因：单品种过拟合反面案例；源码已删（2026-08-28），结论保留
ETH +176% / SOL +26% / BTC -23% / BNB -37% / DOGE -46%
```

### DOGEDualV1 ❌ 已丢弃

```
丢弃原因：多品种崩盘（仅DOGE盈利，ETH/SOL无效）；源码已删（2026-08-28）
DOGE +185% / ETH +9% / SOL -7%
```

---

## 检测结果模板

```
策略：XXX V1
信号：XXX
A 盲参：      ✅ +$X,XXX（N/N 盈）
B 参数稳定：   ✅ 步长 X%-X% 全盈利
C 逐年滚动：   ✅ 2023-2024 全盈利（TEST 内）
CORE×TEST：   ✅ N/N 品种盈 +$X,XXX  PF=X.XX
VOLUME×TEST： ✅ N/N 品种盈 +$X,XXX  PF=X.XX
CORE×VAL：    ✅ N/N 品种盈 +$X,XXX  PF=X.XX  dd=X%（一次性）
VOLUME×VAL：  ✅ N/N 品种盈 +$X,XXX  PF=X.XX  dd=X%（一次性）
集中度：      ⚠️/✅ top 品种占比 X%（无单年 pump 嫌疑）
结论：通过 / 丢弃
```
（以上独立口径数字直接从 `validate_strategy.py` 报告抄录）
